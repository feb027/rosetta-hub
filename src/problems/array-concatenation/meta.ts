import type { ProblemMeta } from '../../types/problem';

export const meta: ProblemMeta = {
  title: 'Array Concatenation',
  slug: 'array-concatenation',
  difficulty: 'easy',
  tags: ['array', 'algorithm'],
  description: `Show how to concatenate two arrays in your language.

If this is as simple as array1 + array2, so be it.

Array concatenation is a fundamental operation that combines two arrays into a single array, with all elements from the first array followed by all elements from the second array.`,
  rosettaCodeUrl: 'https://rosettacode.org/wiki/Array_concatenation',
  createdAt: '2025-11-30',
  previewImage: '/previews/array-concatenation.png',
};
